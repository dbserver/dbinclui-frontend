/*import { render, screen } from '@testing-library/react';
import { DictionaryDbinclui } from './index';
import '@testing-library/jest-dom';

describe('Verificar a página dictionary-DbInclui', () => {
  test('Verificar se o titulo  Dicionario DBInclui está na página', () => {
    render(<DictionaryDbinclui />);
    const mock = screen.getByText('Dicionario DBInclui');
    expect(mock).toBeInTheDocument();
  });
  test('Verificar se o componente reutilizavel está renderizando em tela', () => {
    const { getByText } = render(<DictionaryDbinclui />);
    expect(
      getByText('Lorem Ipsum dolor Lorem Ipsum dolor Lorem Ipsum dolor'),
    ).toBeInTheDocument();
  });
});
*/
export {};
