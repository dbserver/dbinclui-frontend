import { Theme, SxProps } from '@mui/material';

const headingDictionaryDBInclui: SxProps<Theme> = {
  display: 'flex',
  justifyContent: 'center',
  fontWeight: 'bold',
};

const styles = {
  headingDictionaryDBInclui,
};

export default styles;
