import { Theme, SxProps } from '@mui/material';


const categoryContent: SxProps<Theme> = {
  fontSize: '1.14rem',
  mb: '3rem',
  whiteSpace: 'pre-wrap',
};

const styles = {
  categoryContent,
};

export default styles;
